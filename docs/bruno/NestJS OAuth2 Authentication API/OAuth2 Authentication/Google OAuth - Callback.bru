meta {
  name: Google OAuth - Callback
  type: http
  seq: 2
}

get {
  url: {{base_url}}/oauth/google/callback?code={{auth_code}}
  body: none
  auth: inherit
}

params:query {
  code: {{auth_code}}
  ~error: 
}

settings {
  encodeUrl: true
}

docs {
  Handle Google OAuth2 callback with authorization code to get JWT token
}
