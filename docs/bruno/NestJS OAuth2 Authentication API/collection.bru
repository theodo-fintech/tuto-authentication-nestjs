meta {
  name: NestJS OAuth2 Authentication API
}

auth {
  mode: none
}

vars:pre-request {
  base_url: http://localhost:3000
  access_token: 
  auth_code: 
}

docs {
  Postman collection for the NestJS authentication system with JWT and OAuth2 providers (Google, GitHub, Microsoft)
}
